import { View } from "react-native"
import { Header } from "../components/CustomTextComponents"

export const BudgetScreen = () => {
    return (
        <View>
            <Header>Budget</Header>
        </View>
    )
}